#!./testvectorwavecached

# available options with default values (some can only be specified when the
# number of dimensions is known):
#
# epsilon = 1
# mu = 1
# elems = 32 32 ...
# global_refines = 0
# bbox.lower = 0 0 ...
# bbox.upper = 0 0 ...
# start = 0
# end = 1/c = 1*sqrt(epsilon*mu)
# dt_stretch = 0.35
# dt = dt_strech*smallest_edge/c/sqrt(dim)
# vtkprefix = "vectorwavecached_"+gridname+"_EdgeS0.5_"+dim+"D"

#disable vtkoutput
vtkprefix =

[ 2d ]

[ ug ]

[ ug.2d ]

# anything looked up here falls back to the sections [ug], [2d], the global
# section, and the default values.

#elems = 64 64
